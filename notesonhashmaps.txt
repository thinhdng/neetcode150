keys and values can be anything, long as they are consistent(java) -> (string, int), (string, list<string>)
keys and values can be anything in python. 
to add a key and value pair, use the put() method (java)
Collections.max(numsMap.entrySet(), Map.Entry.comparingByValue()).getKey(); returns the key with highest value 
if item in map => python syntax to check if a key is in a hashmap/dict
sorted(map, key=map.get, reverse=True) sort a map of keys using their values in reverse order